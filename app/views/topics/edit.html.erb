<div class="container"
  <div class="row">
    <div class="col-lg-12">
      <h1>記事編集</h1>
      <%= render partial: 'form', locals: { topic: @topic } %>
    </div>
      <%= link_to "削除する", topic_path(@topic), class: "btn btn-primary", method: :delete %>
  </div>
</div>
